/*jshint esversion: 6 */
import {elements} from './views/base.js';

/*
elements.form.addEventListener('submit', e => {
    e.preventDefault();
});
*/

elements.button.addEventListener('click', () => {
    //const value = elements.message.value;
    //console.log(value);
    //elements.add.insertAdjacentHTML('beforeend',`<p>${value}</p><br>`);
    //message.value = '';
});
